### 12.22-12.23
**whitelist/privacy/random_addr/bond_list<单独加不走绑定-接口是啥？>**
1. 在什么时候add/remove/开/关？（每个都得单独讨论 确定并记录）
    - 当前在最开始开了privacy和random_addr,一直不关
> 🐈解决方案：
    - whitelist 开关: 随机开关
    - whitelist满处理: 报满的话，下次加之前清除所有whitelist
    - 🐸[<span style="color:red">TODO</span>] bondlist单加接口，先问研发有无接口，若无接口走正常流程 


**scan测试端 adv配合端**
1. adv端参数不好确定,如何由scan端参数算adv端参数(scan端参数算adv参数)
    - scan_type
    - scan_own_addr_type
    - scan_filter_policy
    - scan_duration(30s)
    - scan_window
    - scan_interval

2. whitelist会和filter_policy 相互影响 - 谁决定谁
   > whitelist决定filter_policy
   因为scan会返回ok，即使扫不到，只要符合whitelist决定filter_policy预期的结果
3. privacy、bondlist和own_addr_type和adv_peer_addr_type相互影响 - 谁决定谁
    > scan_own_addr_type决定adv_peer_addr_type
4. scan_req_notify 4.2没这个变量，相关功能也没怎么测（4.2好像是宏）
    - 🐸[<span style="color:red">TODO</span>] scan_req_notify 问honghui console怎么做的？有无事件什么的？

**action.py中传参方式**
1. 改成字典？
    - 🐸[<span style="color:red">TODO</span>] 后面再做，等随机模式做完之后

**bug复现**
1. 啥时候存？
    > 每个action跑完存, 若失败except前存
2. 存哪儿？
    > 存表格，如下

    | timestamp | dut | action | params | expected_result | actually_result |
    |-----------|-----|--------|--------|-----------------|-----------------|
    |           |     |        |        |                 |                 |
    |           |     |        |        |                 |                 |
    |           |     |        |        |                 |                 |

3. 存的函数定义在哪个模块里？
    > log模块里
4. 复现时读取后如何接入流程？ 
    > 在生成参数模块前接入流程，指定参数和随机参数上层走2套。
5. 普通 case 就可以变成这类文件 -> island 要验证的东西，什么时候做？
    > 先不做，留个接口后面再做。
      暂时遇到失败就停，但是给遇到失败不停留个接口，最终形态是遇到失败不停
      adv scan conn状态都搞个monitor


 
### 12.24
config_index随机必要性
ignore问题 




